# Install entropy calculation library
# Run this script to install scipy for proper entropy calculations

Write-Host "Installing entropy calculation library..." -ForegroundColor Green

# Install scipy which has excellent entropy functions
pip install scipy

Write-Host "Entropy library installation complete!" -ForegroundColor Green
Write-Host "scipy.stats.entropy provides professional entropy calculations" -ForegroundColor Cyan